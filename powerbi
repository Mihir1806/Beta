import pandas as pd

# Extract job name starting with "in_" from the 'Subject' column
def extract_job(subject):
    if isinstance(subject, str) and subject.startswith("in_"):
        return subject
    return None

dataset["Job_Name"] = dataset["Subject"].apply(extract_job)