Objective:
Create a user-friendly web app using Python (Flask) and HTML that allows users to upload multiple Alteryx workflows (.yxmd/.yxmc), extracts formulas from Formula, Multi-Row Formula, and Filter tools, and converts them to Spark SQL.

⸻

🔧 Functionality Requirements:
	1.	File Upload:
	•	Allow users to upload multiple Alteryx workflows (.yxmd/.yxmc) using a file browser.
	•	Only .yxmd and .yxmc formats should be allowed.
	2.	Formula Extraction:
	•	Parse uploaded XML files and extract expressions from:
	•	Formula tools
	•	Multi-Row Formula tools
	•	Filter tools
	3.	Conversion Logic:
	•	Convert Alteryx expressions to Spark SQL by handling:
	•	IF...THEN...ELSE...ENDIF → CASE WHEN THEN ELSE END
	•	Logical operators: AND, OR, NOT
	•	String functions: LEFT(), RIGHT(), UPPER(), LOWER(), Contains()
	•	Date/Time: DateTimeNow() → CURRENT_TIMESTAMP()
	•	Add more pattern mappings as needed
	4.	Interactive Table:
	•	Display results in a table with columns:
	1.	Alteryx Workflow
	2.	Tool Type
	3.	Alteryx Formula
	4.	Converted Spark SQL
	•	Add inline editing for both formulas (original & converted).
	•	Support saving changes in the table.
	5.	Download Option:
	•	Include a “Download as Excel” button to export the table data.

⸻

🎨 UI/UX Requirements:
	•	Clean, responsive layout using HTML + CSS (Bootstrap or custom)
	•	Intuitive flow:
	•	Upload → Convert → View → Edit → Export
	•	Optional enhancements:
	•	✅ Highlight differences in formula (diff view)
	•	✅ Filter/search by workflow or tool
	•	✅ Dark/light mode toggle
